[project]
name = "rag-lab"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [    "anthropic>=0.121.0",
    "openai>=3.3.0",
    "python-dotenv>=1.2.3",
    "requests>=2.34.2",
    ]

[project.scripts]
rag-lab = "rag_lab:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
